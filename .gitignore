# Python caches
__pycache__/
*.py[cod]
*.pyo
.Python

# Terraform generated / sensitive
.terraform/
*.tfstate
*.tfstate.backup
tfplan
*.tfplan
terraform.tfvars           # ← NEVER commit! Use terraform.tfvars.example
terraform.tfvars.json
.terraformrc

# Environment / secrets
.env
.env.*
*.env

# Lambda layer ZIPs (built by CI, not committed)
infrastructure/layers/*.zip
infrastructure/layers/zips/

# Claude local settings
.claude/

# macOS
.DS_Store
Thumbs.db
